##taxa = float(input("Digite o valor da taxa: "))
##valor_reais = float(input("Digite o valor em reais: "))
##
##valor = valor_reais/taxa
##
##print(f"Valor convertido pela taxa: ${valor:.2f}")

##peso = float(input("Digite o seu peso(em kg): "))
##altura = float(input("Digite a sua altura(em m): "))
##
##imc = peso/(altura**2)
##
##print(f"Valor do IMC é: {imc:.2f}")

#def nome_da_funcao(parametro1,parametro2,...):
    #corpo da função
    #return valor_de_retorno #opcional

##def nada():
##    '''ela não faz nada'''
##    return print("nada")
### evitar de colocar input e print nas defs

def area_coroa(r1,r2):
    '''Função para calcula a área da coroa, r1 raio externo, r2 raio interno'''
    area1 = 3.14159265*(r1**2)
    area2 = 3.14159265*(r2**2)
    return area1-area2

re = float(input(f"Digite o valor do raio externo(em cm): "))
ri = float(input(f"Digite o valor do raio interno(em cm): "))
while ri>re:
    print("O raio interno não pode ser maior que o raio externo!")
    ri = float(input(f"Digite o valor do raio interno(em cm): "))
#Requisição de dados

print(f"O valor da área da coroa é de: {area_coroa(re,ri):.2f}cm²")
#imprimindo o processamento dos dados

#na prova ele vai cobrar funções, fazer funções das mais bestas possíveis
